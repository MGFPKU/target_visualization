# Environment

- **Do not install random libraries.** The main conda environment already has `fastexcel` and other common dependencies. Use `.venv/bin/python` for running code, and check what's available before suggesting new packages.
- The `.venv` is the active virtual environment.
